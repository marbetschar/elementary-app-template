test('CounterWidgetUITests', executable(
  meson.project_name() + '-ui-tests-counter',
  src_dir / 'CounterWidget.vala',
  src_dir / 'Counter.vala',
  'CounterWidgetUITests.vala',
  dependencies: deps,
  install: false
))